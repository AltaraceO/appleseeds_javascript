const theArr2 = [10, 30, 40, 60, 80, 1000];

// function equality(arrVal, num) {
//   return arrVal === num;
// }

function myFind(arr) {
  for (let i = 0; i < arr.lenght; i++) {
    console.log(arr);

    // if (callback(arr[i], num)) {
    //   return `yes`;
    // }
  }
}

myFind(theArr2);

// console.log(findResult);
